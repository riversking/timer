spring:
  application:
    name: user-server
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      label: master
      name: user-server
    bus:
      enabled: true