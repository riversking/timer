server:
  port: 7080
  http2:
    enabled: true
  undertow:
    threads:
      io: 16
      worker: 256
    buffer-size: 1024
    direct-buffers: true



spring:
  application:
    name: nba-server
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8
    default-property-inclusion: non_empty
  datasource:
    name: nba-server
    url: jdbc:mysql://${MYSQL_HOST:139.9.206.164}:${MYSQL_PORT:3306}/timer_nba?useUnicode=true&characterEncoding=UTF8&useSSL=false&serverTimezone=GMT%2B8&allowPublicKeyRetrieval=true
    username: root
    password: King1992!
    # 使用hikari数据源
    type: com.zaxxer.hikari.HikariDataSource
    driver-class-name: com.mysql.cj.jdbc.Driver
    hikari:
      allow-pool-suspension: true
      minimum-idle: 10
      maximum-pool-size: 20
      idle-timeout: 180000
      max-lifetime: 30000
      connection-timeout: 30000
      connection-test-query: SELECT 1
  redis:
    host: ${REDIS_HOST:139.9.206.164}
    port: ${REDIS_PORT:6379}
    password: king1278
    timeout: 2000ms
    database: 0
    jedis:
      pool:
        max-active: 300
        max-idle: 100
        max-wait: -1s
  rabbitmq:
    host: 139.9.206.164
    port: 5672
    username: guest
    password: guest

mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  typeAliasesPackage: com.rivers.nba.model
  global-config:
    #    id: 0  #0:数据库ID自增   1:用户输入id  2:全局唯一id(IdWorker)  3:全局唯一ID(uuid)
    #    db-column-underline: false
    #    refresh-mapper: true
    db-config:
      id-type: auto
  configuration:
    map-underscore-to-camel-case: true
    cache-enabled: true #配置的缓存的全局开关
    lazyLoadingEnabled: true #延时加载的开关
    multipleResultSetsEnabled: true #开启的话


eureka:
  instance:
    instance-id: ${spring.application.name}:${server.port}:${random.value}
    prefer-ip-address: true
    ip-address: 139.9.206.164
    health-check-url-path: /actuator/health
  client:
    serviceUrl:
      defaultZone: http://139.9.206.164:8761/eureka/

logging:
  level:
    com.rivers: debug
  #  spring-admin 读取应用的日志 关键
  file:
    path: ./logs/${spring.application.name}/all.log
    name: ./logs/${spring.application.name}/all.log

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
    logfile:
      enabled: true
    loggers:
      enabled: true
    bus-refresh:
      enabled: true

security:
  oauth2:
    client:
      client-id: admin
      client-secret: secret
      scope: server
    resource:
      token-info-uri: http://139.9.206.164:9080/oauth/check_token
      service-id: oauth-server
#      user-info-uri:

hystrix:
  shareSecurityContext: true

xxl:
  job:
    admin:
      addresses: http://139.9.206.164:8081/xxl-job-admin
    executor:
      appname: nba-service-executor
      address:
      ip:
      port: 9001
      logpath: /data/applogs/xxl-job/jobhandler
      logretentiondays: 30
    accessToken: token

nba:
  key: a2e4941f622f4398b8cec175b3b51b6b
  url: https://api.sportsdata.io/v3/nba/scores/json/
