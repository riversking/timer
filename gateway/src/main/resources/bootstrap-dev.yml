spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      label: master
      name: gateway-server
    bus:
      enabled: true
  rabbitmq:
    host: 139.9.206.164
    port: 5672
    username: guest
    password: guest

#
#spring:
#  application:
#    name: gateway
#  cloud:
#    config:
#      uri: http://localhost:8888
#      label: master
#      profile: dev
#      name: gateway-server