spring:
  application:
    name: gateway
  cloud:
    config:
      discovery:
        enabled: true
        service-id: config
      label: master
      name: gateway-server
    bus:
      enabled: true

#
#spring:
#  application:
#    name: gateway
#  cloud:
#    config:
#      uri: http://localhost:8888
#      label: master
#      profile: dev
#      name: gateway-server