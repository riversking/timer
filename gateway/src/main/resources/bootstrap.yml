server:
  port: 10500

spring:
  application:
    name: gateway
  profiles:
    active: local

logging:
  level:
    org.springframework.cloud.gateway: info
    org.springframework.http.server.reactive: DEBUG
    org.springframework.web.reactive: DEBUG
    reactor.ipc.netty: DEBUG

eureka:
  instance:
    instance-id: ${spring.application.name}:${server.port}
    prefer-ip-address: true
    ip-address: localhost
    health-check-url-path: /actuator/health
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

#security:
#  oauth2:
#    client:
#      client-id: admin
#      client-secret: secret
#      scope: server
#      access-token-uri: http://localhost:9080/oauth/token
#      user-authorization-uri: http://localhost:9080/oauth/authorize
#    resource:
#      token-info-uri: http://localhost:9080/oauth/check_token
#    authorization:
#      check-token-access: http://localhost:9080/oauth/check_token

management:
  endpoints:
    web:
      exposure:
        include: "*"
  endpoint:
    health:
      show-details: ALWAYS
